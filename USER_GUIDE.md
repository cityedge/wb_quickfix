# WB QuickFix 1.0.1 ユーザーガイド

## 1. WB QuickFix について

WB QuickFix は、画像のホワイトバランス修正を中心にした小型の画像レタッチソフトです。

写真や生成画像では、画像全体が黄色っぽい、青っぽい、緑っぽい、マゼンタっぽいなど、色かぶりが発生することがあります。WB QuickFix では、画像内の「本来は白またはグレーである場所」を指定することで、その場所を無彩色の基準としてホワイトバランスを調整できます。

ホワイトバランス以外にも、日常的な修正作業を別ソフトへ持ち出さずに済むよう、明るさ・コントラスト・彩度・色相・ガンマの基本補正を備えています。

このソフトは多機能な総合画像編集ソフトを目指したものではありません。色かぶり修正と軽い全体補正を、少ない操作で終わらせることを主目的としています。

---

## 2. 対応画像形式

読み込みと保存に対応している形式は次の3種類です。

- JPEG / JPG
- PNG
- BMP

RAW 画像には対応していません。

### JPEG

一般的な写真向けの非可逆圧縮形式です。保存時に `JPEG品質` を 70 ～ 100 の範囲で指定できます。

数値が高いほど圧縮による劣化が少なく、ファイルサイズは大きくなります。初期値は 95 です。

### PNG

可逆圧縮形式です。Alpha チャンネルを持つ PNG は、可能な限り透明度を維持したまま処理・保存します。

保存時に `PNG圧縮レベル` を 0 ～ 9 で指定できます。

- 0：圧縮をほとんど行わない。保存が速く、ファイルが大きい
- 6：標準的な設定
- 9：最大圧縮。保存に時間がかかる場合がある

PNG の圧縮レベルを変更しても画質は変わりません。ピクセル値は同じで、圧縮率と保存時間だけが変化します。

### BMP

BMP は読み込み・保存に対応しています。保存時は互換性を優先し、24bit RGB で出力します。

透明度を持つ画像を BMP へ保存する場合、透明部分は白背景へ合成されます。

---

## 3. 起動

ソース版を使う場合は、フォルダ内の `run.bat` をダブルクリックします。

初回のみ Python 仮想環境 `.venv` を作成し、PySide6 / NumPy / Pillow をインストールします。その後、WB QuickFix が起動します。

Python 3.10 以降が必要です。

---

## 4. 画面構成

画面は大きく2つに分かれています。

左側が画像表示ペイン、右側が操作ペインです。

右側には、上からおおむね次の順に操作項目があります。

1. 開く / 1回戻す / オールリセット
2. ホワイトバランス（自動WB / 白指定 / 肌指定 / 手動調整）
3. 基本補正
4. 保存設定
5. Original / ズーム / 全体表示
6. 言語切り替え

すべての操作系を右側へ集約しています。

---

## 5. 画像を開く

画像を開く方法は3通りあります。

### 方法1：`開く` ボタン

右上の `開く` を押し、JPEG / PNG / BMP を選択します。

### 方法2：画像ペインをクリック

まだ画像が読み込まれていない場合に限り、左側の画像ペインを左クリックすると、`開く` ボタンと同じファイル選択画面が開きます。

### 方法3：ドラッグ＆ドロップ

JPEG / PNG / BMP ファイルを左側の画像ペインへドラッグ＆ドロップします。

新しい画像を開くと、画像補正パラメータはすべて初期値へ戻ります。

---

## 6. 自動WB

`自動WB` は、画像内の白基準を手動でクリックせず、元画像全体から自動的に推定する機能です。

### 基本操作

1. 画像を開きます。
2. `自動WB` を押します。
3. 元画像全体を解析し、白またはグレーらしい領域を複数抽出して、その色分布の中心を白基準として使用します。
4. 推定結果から、色温度・色偏差・自然補正が自動的に更新されます。

自動WBも白スポイトと同様、補正済み画像を次の計算基準にはしません。解析対象は常に「読み込んだままの元画像」です。

### どのように白を探すか

処理速度を保つため、元画像をそのまま全画素精査するのではなく、解析用に縮小した画像を小さな領域へ分けて評価します。主に次の要素を組み合わせて白候補群を作ります。

- 明るさが比較的高い
- 強い色の物体より、白・グレーとして説明しやすい
- 完全な白飛びだけで構成された領域をできるだけ避ける
- 単一ピクセルではなく、ある程度まとまった領域として安定している

重要なのは、候補の中から「もっとも無彩色に近い1か所」を選ばないことです。画像全体に赤かぶりや黄かぶりがある場合、本来白い複数の場所も同じ方向へ色がずれます。このため、自動WBでは候補群を色度空間上に並べ、明らかに外れた色の領域を除いたうえで、その分布の中心を推定します。

たとえば白候補群の大部分が赤方向へずれている場合、その赤方向へのずれ自体を照明色とみなし、中心色をニュートラルへ戻す方向に補正します。従来より強い全体的な色かぶりにも反応しやすい設計です。

最終的には、推定された中心色を `白を指定` でクリックした場合と同じホワイトバランス計算へ渡します。したがって、結果は隠れた補正として保持されず、画面上の色温度・色偏差・自然補正へ反映されます。

### 暖色照明について

WB QuickFix の自動WBは、暖色照明を写真の雰囲気として積極的に残す設計ではありません。

たとえば電球色の室内で、本来白い壁や白い紙が黄色く写っている場合、その領域を白基準と推定できれば、暖色かぶりを除去して通常の白へ近づける方向に補正します。これは本ソフトでは意図した挙動です。

### 自動WBが合わない場合

自動WBは画像内容から白基準を推定するため、常に正しいとは限りません。たとえば次のような画像では判断が難しくなることがあります。

- 白やグレーに相当する物体がほとんど存在しない
- 画面の大部分を単一の強い色が占めている
- 非常に暗い画像
- 強いネオン、色付き照明、色付きハイライトが画面の大部分を占める
- 意図的な色調演出をそのまま残したい画像

結果が合わない場合は、`白を指定` で本来白またはグレーである場所を手動指定してください。自動WBを実行した後でも、手動の白指定でその結果を置き換えられます。

---

## 7. 白を指定する

WB QuickFix の中心機能です。

### 基本操作

1. `白を指定` を押します。
2. マウスポインタがスポイト用の状態になります。
3. 画像内の「本来は白またはグレーである場所」をクリックします。
4. 色温度、色偏差、自然補正の値が自動的に更新されます。

このとき、クリックした場所を真っ白にするわけではありません。

たとえば暗いグレーをクリックした場合、その場所の明るさは大きく変えず、「本来は無彩色のグレーだった」とみなして色かぶりを補正します。

### サンプリング範囲

クリックした1ピクセルだけを使うのではなく、クリック地点を中心とした 11 × 11 ピクセルの範囲を参照します。

その範囲の RGB 中央値を使うため、単一ピクセルのノイズ、JPEG圧縮ノイズ、小さなハイライトなどの影響を受けにくくしています。

### どこをクリックすればよいか

適している場所：

- 白い紙
- 白い壁
- 白い衣服
- 無彩色のグレー
- 白またはグレーと分かっている物体

避けた方がよい場所：

- 色付きの白に見える物体
- 夕焼けや照明色を意図的に反射している場所
- 極端に暗い場所
- 完全に白飛びしている場所
- 強い色の反射光を受けている場所

暗すぎる点や白飛びに近い点を指定した場合は、ステータス欄に注意表示が出ることがあります。

### 白指定をやり直した場合

白指定は積み重なりません。

たとえば A 地点で白指定した後、B 地点でもう一度白指定した場合、B 地点のクリックは「A 地点で補正済みの画像」に対して行われるのではありません。

毎回、読み込んだままの元画像上の B 地点をサンプリングし、その結果から新しい色温度・色偏差・自然補正を求めます。

そのため、複数の候補地点を順番にクリックしながら、どの基準が自然に見えるか比較できます。

---

## 8. 肌を指定（実験）

`肌を指定（実験）` は、肌色を基準にした補助的な色補正です。

白やグレーとは異なり、肌色には「この RGB が唯一の正解」という値がありません。肌の色、照明、日焼け、血色、メイクなどによって正常な範囲が大きく変わります。

そのため、この機能は強制的に特定の肌色へ変換するのではなく、複数の肌色クロマ基準の中から近いものを選び、明らかな色かぶりを弱く戻す方向で動作します。

使い方：

1. `肌を指定（実験）` を押します。
2. 顔や手など、肌が比較的均一に見える場所をクリックします。
3. 色温度、色偏差、自然補正が更新されます。

白スポイトほど明確な正解がある処理ではないため、結果が合わない場合は白スポイトまたは手動調整を使用してください。

---

## 9. ホワイトバランスの手動調整

### 色温度

範囲：-100 ～ +100

0 が無補正です。

- マイナス側：寒色方向
- プラス側：暖色方向

絶対的な Kelvin 値ではなく、読み込んだ画像に対する相対補正です。

### 色偏差

範囲：-100 ～ +100

0 が無補正です。

主に緑 ⇔ マゼンタ方向の色かぶりを調整します。

### 自然補正

範囲：0 ～ 100

0 では自然補正を行いません。

値を上げると、ホワイトバランス補正による極端な変化を穏やかにするため、内部で複数の処理を組み合わせます。

主な目的は次のとおりです。

- 暗部で補正が強くなりすぎるのを抑える
- ハイライトで色が不自然に飛ぶのを抑える
- ホワイトバランス変更による過度な彩度上昇を抑える
- 肌色らしい領域への極端な補正を弱く保護する

「白指定すると数学的には中立になったが、見た目として少し強すぎる」という場合に上げると有効です。

---

### ホワイトバランスの Reset

`ホワイトバランス` 見出し右側の `Reset` を押すと、次の3項目だけを初期値へ戻します。

- 色温度：0
- 色偏差：0
- 自然補正：0

明るさ・コントラスト・彩度・色相・ガンマなどの基本補正は変更しません。ホワイトバランスだけをやり直したい場合に使用してください。

この操作も `1回戻す` の対象です。

---

## 10. 基本補正

ホワイトバランスだけでは仕上がらない場合に使用します。

### 明るさ

範囲：-100 ～ +100

0 が無補正です。

画像全体を暗く、または明るくします。マイナス側は暗くするための補正を強めてあり、`-100` では大幅に暗くなります。プラス側はハイライトの白飛びを増やしすぎないよう、比較的穏やかな設定です。

### コントラスト

範囲：-100 ～ +100

0 が無補正です。

マイナス側では明暗差を小さくし、プラス側では明暗差を大きくします。

### 彩度

範囲：-100 ～ +100

0 が無補正です。

- -100：ほぼ無彩色
- プラス側：色を鮮やかにする

### 色相

範囲：-180° ～ +180°

0° が無補正です。

画像全体の色相を回転させます。ホワイトバランスの `色偏差` とは別の処理です。

### ガンマ

範囲：0.00 ～ 3.00

1.00 が無補正です。

一般的なガンマ値をそのまま表示します。UI では 0.00 まで選択できますが、計算上 0 は使用できないため、内部では最小値へ置き換えて処理します。

### 基本補正の Reset

`基本補正` 見出し右側の `Reset` を押すと、次の5項目だけを初期値へ戻します。

- 明るさ：0
- コントラスト：0
- 彩度：0
- 色相：0°
- ガンマ：1.00

ホワイトバランス側の色温度・色偏差・自然補正は変更しません。基本補正だけやり直したい場合に使用してください。

この操作も `1回戻す` の対象です。

---

## 11. スライダー操作

各項目はスライダーと数値入力の両方で変更できます。

細かい値を合わせたい場合は数値フォームを使ってください。

各スライダーをダブルクリックすると、その項目だけ初期値へ戻せます。

全項目をまとめて戻す場合は `オールリセット` を使用します。

---

## 12. Original との比較

補正前後を比較する方法は2つあります。

### Original ボタン

右ペインの `Original` ボタンを押している間だけ、読み込んだままの元画像を表示します。

ボタンを離すと補正後へ戻ります。

### 画像ペインを右クリック

画像が表示されている状態で、左側の画像ペインを右クリックしている間も Original 表示になります。

右ボタンを離すと補正後へ戻ります。

この操作は、ホワイトバランスの微妙な差や、明るさ・彩度の変化を素早く比較するのに便利です。

---

## 13. 1回戻す

`1回戻す` は、直前の編集状態へ1回だけ戻します。

ショートカットは `Ctrl + Z` です。

多段階の編集履歴を保持する機能ではありません。さらに前の状態へ何段階も遡ることはできません。

---

## 14. オールリセット

右ペイン上部の `オールリセット` を押すと、現在の画像に対する補正をすべて初期状態へ戻します。

初期値：

- 色温度：0
- 色偏差：0
- 自然補正：0
- 明るさ：0
- コントラスト：0
- 彩度：0
- 色相：0°
- ガンマ：1.00

元画像そのものが変更されることはありません。

---

## 15. ズームと表示

右ペイン下部に表示操作があります。

- `−`：縮小
- `+`：拡大
- `全体表示`：画像全体がペイン内へ収まる倍率にする

マウスホイールでも拡大・縮小できます。

画像を拡大した状態では、ドラッグして表示位置を移動できます。

---

## 16. 保存形式の選択

`名前を付けて保存` を押す前に、右ペインの `保存形式` で形式を選びます。

選択肢：

- JPEG
- PNG
- BMP

新しい画像を開いた直後は、原則として入力画像と同じ形式が自動選択されます。

例：

- JPEG を開く → JPEG が初期選択
- PNG を開く → PNG が初期選択
- BMP を開く → BMP が初期選択

保存形式そのものはアプリ終了後に記憶しません。新しく開いた画像の形式が優先されます。

---

## 17. JPEG品質

保存形式が JPEG の場合だけ `JPEG品質` が表示されます。

範囲：70 ～ 100

初期値：95

JPEG品質はアプリ終了後も保存され、次回起動時に前回値を復元します。

JPEG は非可逆圧縮なので、同じ画像を何度も JPEG 保存して再編集すると劣化が重なります。可能であれば、元画像を毎回読み直して編集してください。

---

## 18. PNG圧縮レベル

保存形式が PNG の場合だけ `PNG圧縮レベル` が表示されます。

範囲：0 ～ 9

初期値：6

PNG圧縮レベルもアプリ終了後に保存され、次回起動時に前回値を復元します。

この設定は画質を変更しません。

---

## 19. BMP 保存

BMP を選択した場合、JPEG品質やPNG圧縮レベルの設定は表示されません。

保存時は一般的な 24bit RGB BMP として出力します。

入力画像に透明度がある場合、透明部分は白背景へ合成されます。

---

## 20. 名前を付けて保存

WB QuickFix は、元画像を自動的に上書きしません。

`名前を付けて保存` を押し、出力先とファイル名を指定します。

初期ファイル名は、元画像名に `_wb` を付けた名前になります。

例：

```text
photo.jpg
↓
photo_wb.jpg
```

保存形式プルダウンで選択した形式が優先されます。

---

## 21. 画像処理の考え方

WB QuickFix では、補正後画像へさらに補正結果を焼き込んでいく方式を採用していません。

内部では常に、

```text
元画像
+ 現在の全パラメータ
↓
再計算
```

という構造です。

このため、スライダーを何度動かしても、前の計算結果へさらに計算を重ねることによる劣化は発生しません。

白スポイトや肌スポイトについても、クリック位置のサンプリングは常に元画像から行います。

---

## 22. プレビューと保存画質

大きな画像でも操作しやすくするため、画面プレビューは最大辺 1800 px の縮小画像を使ってリアルタイム処理します。

保存するときは、縮小プレビューを保存するのではなく、元のフル解像度画像から全処理をやり直します。

そのため、プレビューが縮小表示されていても、保存画像の解像度は元画像と同じです。

---

## 23. EXIF Orientation とメタデータ

JPEG 等に EXIF Orientation が含まれている場合、読み込み時に正しい向きへ反映します。

保存時には、可能な範囲で次の情報を引き継ぎます。

- ICC Profile
- EXIF
- DPI

画像形式や元ファイルの状態によっては、すべてのメタデータを完全に保持できない場合があります。

---

## 24. 256色・16色などのパレット画像

256色 PNG、16色 PNG、パレット BMP なども読み込めます。

ただし、編集時には通常の RGB / RGBA へ展開します。

ホワイトバランスやガンマ補正を行うと新しい中間色が必要になるため、保存時に自動的に元のパレット色数へ戻すことはしません。

したがって、元が 256色 PNG であっても、保存後は通常のフルカラー PNG になる場合があります。

---

## 25. 保存される設定

次の設定は、アプリ終了後も保存されます。

- JPEG品質
- PNG圧縮レベル
- UI言語

Windows では PySide6 の `QSettings` を通じて、通常はユーザーごとの設定領域に保存されます。

画像補正値は保存されません。新しい画像を開くと初期値へ戻ります。

保存形式も永続化しません。入力画像形式を初期値として使用します。

---

## 26. UI 言語

右ペイン最下部の言語ボタンで、日本語と English を切り替えられます。

選択した言語は保存され、次回起動時に復元されます。

---

## 27. キーボードショートカット

| 操作 | ショートカット |
|---|---|
| 開く | Ctrl + O |
| 名前を付けて保存 | Ctrl + Shift + S |
| 1回戻す | Ctrl + Z |
| オールリセット | Ctrl + R |
| スポイト解除 | Esc |

---

## 28. おすすめの作業手順

一般的な画像では、次の順番が扱いやすいです。

1. 画像を開く
2. まず `自動WB` を試す
3. Original と比較する
4. 自動WBが合わない場合は、白またはグレーの候補地点を探して `白を指定` する
5. 必要なら別の白候補を再度クリックして比較する
6. 色温度・色偏差を微調整
7. 補正が強く感じる場合は自然補正を調整
8. 明るさ・コントラストを調整
9. 必要な場合だけ彩度・色相・ガンマを調整
10. 保存形式を選ぶ
11. JPEG品質またはPNG圧縮レベルを確認
12. `名前を付けて保存`

人物画像で白基準がない場合は、肌スポイトを実験的に試すことができます。

---

## 29. ホワイトバランスがうまく決まらない場合

### 白指定後に色が不自然になる

クリックした場所が実際には無彩色でない可能性があります。別の白またはグレー候補を試してください。

### 白い場所が複数あり、どこを選ぶか迷う

候補 A、候補 B を順番にクリックして比較してください。各クリックは常に元画像を基準に再計算されるため、前回のスポイト結果が累積することはありません。

### 暗い場所しか基準がない

暗部はノイズや色むらの影響が大きくなりやすいため、可能ならもう少し明るい無彩色部分を探してください。

### 白飛び部分しかない

完全に白飛びした領域は RGB 各チャンネルの色差が失われているため、ホワイトバランスの基準として有効な情報が少なくなります。

### 人物の顔色がおかしくなる

自然補正を上げるか、色温度・色偏差を手動で少し戻してください。白基準がない場合は肌スポイトも試せますが、実験機能のため必ず良い結果になるとは限りません。

---

## 30. 現在対応していない主な機能

WB QuickFix 1.0.1 では、次の機能は扱いません。

- RAW 現像
- トリミング
- 回転・傾き補正
- シャープ
- ノイズ除去
- トーンカーブ
- HSL 個別補正
- 部分補正
- レイヤー
- 多段階 Undo 履歴
- バッチ処理
- パレット色数を指定した減色保存

これらを省くことで、ホワイトバランス修正と日常的な全体補正へ操作を集中させています。

---

## 31. ライセンス

WB QuickFix は MIT License で公開されています。

```text
Copyright (c) 2026 cityedge
```

ライセンス全文は同梱の `LICENSE` を参照してください。

依存ライブラリには、それぞれのライセンスが適用されます。
