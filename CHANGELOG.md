# Changelog

## 1.0.6 - 2026-09-22

- 色温度の調整範囲を `-100 ～ +100` から `-200 ～ +200` へ拡張
- 強い暖色・寒色かぶりを持つ画像で、自動WBや白スポイトが上限・下限へ早期に張り付く問題を改善
- 拡張した色温度範囲でも実際の補正量が飽和しないよう、内部RGBゲインの許容範囲を拡大
- 自動WBの白候補群中心方式、元画像基準の非累積処理など既存仕様は維持

## 1.0.5 - 2026-09-22

- 日本語UIの `肌を指定（実験）` を `肌色を指定（実験）` へ変更
- English UI は `Pick Skin Tone (Exp.)` とし、実験機能表記を短縮
- 画像ペインの右クリック比較を分割表示へ変更
- 右クリック位置より左側に処理後画像、右側に Original を表示
- 右ボタンを押したまま左右へドラッグして分割境界を移動可能
- 右ボタンを離すと通常の処理後表示へ復帰
- 右ペインの `Original` ボタンは従来どおり、押している間だけ画像全体を Original 表示

## 1.0.4 - 2026-09-21

- 自動WBの推定方式を更新
- 単一の「最も白に近い領域」を選ぶ方式から、複数の白・グレー候補領域の色度分布の中心を推定する方式へ変更
- 白候補群から明らかな色の外れ値を除外し、残った候補群のロバストな中心を白基準として使用
- 画像全体に赤・黄・青などの強い色かぶりがある場合でも、候補群全体のずれを照明色として補正しやすく改善
- 自動WBは従来どおり常に読み込んだままの元画像を解析し、結果を色温度・色偏差・自然補正へ反映

## 1.0.3 - 2026-09-21

- `ホワイトバランス` 見出し右側に `Reset` ボタンを追加
- ホワイトバランス側の `Reset` で、色温度・色偏差・自然補正だけを初期値へ戻し、基本補正は維持
- ホワイトバランス側の `Reset` も `1回戻す` の対象
- 右ペイン上部の従来の `リセット` を `オールリセット`（English: `Reset All`）へ改称
- `オールリセット` はホワイトバランスと基本補正を含む全補正を初期化

## 1.0.2 - 2026-09-21

- `基本補正` 見出し右側に `Reset` ボタンを追加
- `Reset` で明るさ・コントラスト・彩度・色相・ガンマだけを初期化し、ホワイトバランス設定は維持
- 基本補正の `Reset` も `1回戻す` の対象
- 明るさのマイナス側を大幅に強化。`-100` は従来の約 -1.5 EV 相当から -4 EV 相当へ変更
- 明るさのプラス側は従来どおり最大 +1.5 EV 相当を維持

## 1.0.1 - 2026-09-21

- `自動WB` を追加
- 元画像全体を縮小解析し、明るく白に近い領域を自動推定して白基準として使用
- 自動WBの結果を、色温度・色偏差・自然補正の見えるパラメータへ反映
- 自動WBも常に読み込んだままの元画像を基準に計算し、補正を累積しない
- 暖色照明は、白候補を推定できた場合に通常の白へ近づける方向で補正

## 1.0.0 - 2026-09-20

WB QuickFix の初回公開版です。

主な機能：

- JPEG / PNG / BMP の読み込み・保存
- 白またはグレーの基準点を指定するホワイトバランス補正
- 実験的な肌色基準補正
- 相対色温度、色偏差、自然補正
- 明るさ、コントラスト、彩度、色相、ガンマの基本補正
- 元画像を常に基準とする非破壊型の再計算
- 1回 Undo、全リセット、Original 比較
- 画像ペインへのドラッグ＆ドロップ
- 画像未読込時の画像ペインクリックによる「開く」
- 画像ペインの右クリック押下中だけ Original を表示
- JPEG 品質、PNG 圧縮レベル、UI 言語の設定保存
- 日本語 / English UI
- ダークテーマ
